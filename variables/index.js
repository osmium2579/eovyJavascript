a = 221
b = a - 5
console.log(b)

let a = 221
let b = a - 5
console.log(b)

let a = 221
let b = a - 5
a = 4
console.log(b, a);

const a = 221
const b = a - 5
console.log(b)

/*
변수를 만들고 싶을 때 기본적으로 작동하는 구조

1.Create
2.Initialize
3.Use

*/

/*
변수를 초기화 전 let을 앞에 사용해야 함.
변수를 초기화하거나 생성할 때에는 let을 써야하지만 그 외 이미 선언된 변수에는 쓰지않는다.
*/

/*
const(상수) / constant의 약어.
상수는 안정적이라는 의미.
let이 아닌 const로 변수를 생성하면 변수에 새로운 값이 저장될 수 없다.
다른 사람이 나의 코드를 변경하지 못하게 하려면 const를 사용.
실제로 실행해보면 error가 발생함.
*/